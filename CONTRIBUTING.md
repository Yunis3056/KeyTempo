# KeyTempo 贡献指南

欢迎提交问题、文档修正和代码改进。普通错误修复可以直接提交 Pull Request；影响用户操作流程或引入新依赖的变更，建议先通过 Issue 说明场景和取舍。

## 开发准备

开发环境和发布命令见 [BUILD.md](BUILD.md)。界面使用 WPF，系统输入与计时逻辑应尽量与界面分离，方便在不触发真实输入的情况下验证。统一使用 [CONTEXT.md](CONTEXT.md) 中的动作、周期、暂停、停止等术语。

## 提交问题

请提供系统与应用版本、配置中的动作类型 / 间隔、最小复现步骤、预期结果和实际结果。涉及显示位置时，补充显示器数量、缩放比例以及窗口是否移动过。日志、配置和截图可能包含窗口名称或个人信息，请提交前检查。

## 提交代码

1. 保持一个 Pull Request 聚焦一个问题；解释用户能观察到的行为变化。
2. 新增或修改核心计时、窗口验证、按键释放逻辑时，添加能覆盖该行为的测试；文档和外观小修不要求机械增加测试。
3. 完成 Release 构建及相关测试。输入测试使用测试窗口并限制执行次数；不要把真实用户的窗口作为无人值守测试目标。
4. 界面改动附上截图；影响配置格式时说明迁移方式；影响公开使用方法时更新中文文档。
5. 不提交构建目录、个人配置、下载的工具、访问令牌或签名证书。

Pull Request 描述包含问题、最终解决方式和验证结果即可。未经维护者安排，不需要重新格式化整个项目。

## 许可证与协作

贡献默认采用项目的 MIT 许可证。请确认有权提交相关代码和素材，并保留必要的第三方声明。讨论遵循 [行为准则](CODE_OF_CONDUCT.md)。
