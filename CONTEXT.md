# 自动输入

自动输入以一组可独立计时的动作，帮助用户在指定桌面窗口中重复键盘或鼠标操作。

## 领域语言

**动作（Action）**：一个可独立启用、配置计时并重复执行的键盘操作或鼠标位置序列。
_Avoid_：任务、宏脚本

**运行（Run）**：从开始到停止之间，同一组启用动作的执行过程；其间可以暂停和继续。
_Avoid_：周期、单次点击

**周期（Cycle）**：某个动作的一次完整执行及其相邻执行之间的计时单位；鼠标动作的一次完整执行可以包含多个位置。
_Avoid_：运行、位置

**目标窗口（Target Window）**：动作绑定并核对的桌面窗口，是键盘输入的接收窗口，也是窗口相对坐标的参考窗口。
_Avoid_：后台接收器、自动激活窗口

**屏幕绝对坐标（Screen Coordinates）**：以整个桌面为参考的鼠标位置；不会随目标窗口移动而变化。
_Avoid_：窗口坐标、按钮坐标

**窗口相对坐标（Window Coordinates）**：以目标窗口为参考的鼠标位置；窗口位置变化时，所对应的屏幕位置随之变化。
_Avoid_：屏幕坐标、控件识别

**暂停（Pause）**：暂时中断运行并保留动作的运行进度，以便继续。
_Avoid_：停止、重置

**停止（Stop）**：结束当前运行并清除本轮计时与计数，下次开始形成新的运行。
_Avoid_：暂停、隐藏到托盘
